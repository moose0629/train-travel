import { BaseTrainDirective } from './base-train.directive';

describe('BaseTrainDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseTrainDirective();
    expect(directive).toBeTruthy();
  });
});
